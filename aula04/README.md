# Padrões Estruturais

Aqui vamos discutir como utilizar os padrões estruturais. Para isso vamos considerar a aplicação base em `base`.

Por questão de simplicidade, todos os arquivos de classe foram declarados no mesmo arquivo. ISSO NÃO É ENCORAJADO EM UM PROJETO, aqui é apenas por facilidade para colocar o projeto para ser executado.